Added fields_input_iterator that is now used in db-code.cpp. 

The fields_input_iterator works fine. The textfoi table has one less row tan the devicefoi and mdrfoi tables. Detemine which row is missing and why.
Then test the ~/fda code without transactions and lastly with transactions. 
