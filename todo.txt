Added fields_input_iterator that is now used in db-code.cpp. 

The fields_input_iterator seems to works fine. I suspect the bugg is in is_new_record logic--maybe because the table is empty. Check also the maude_seek_iterator. 
